import React from "react";

const ProfilePage = () => {
  const handleLogout = () => {};
  return (
    <div>
      <p onClick={handleLogout}>Logout</p>
    </div>
  );
};

export default ProfilePage;
